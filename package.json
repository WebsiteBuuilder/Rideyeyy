{
  "name": "rideey-discord-bot",
  "version": "1.0.0",
  "description": "Route Cash economy Discord bot for Rideey",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && node scripts/copy-migrations.js",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "migrate": "npm run build && node -e \"require('dotenv').config(); const { initDatabase, runMigrations, closePool } = require('./dist/database'); (async () => { await initDatabase(); await runMigrations(); await closePool(); process.exit(0); })().catch((e) => { console.error(e); process.exit(1); });\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "decimal.js": "^10.4.3",
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "pg": "^8.13.1",
    "pg-connection-string": "^2.13.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/node-cron": "^3.0.11",
    "@types/pg": "^8.11.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
